export interface DemoIds {
  workspaceId: string;
  appId: string;
  releaseId: string;
}

export const demoIds: DemoIds = {
  workspaceId: "workspace_demo",
  appId: "app_demo",
  releaseId: "release_demo"
};
